{
  "description": "Request to retrieve data from external source (https://stacksonchain.com/)",
  "title": "StatusChecks",
  "additionalProperties": false,
  "type": "object",
  "required": ["url", "method", "disabled", "table", "query"],
  "properties": {
    "url": {
      "type": "string",
      "description": "The base URL of the stacksonchain API"
    },
    "method": {
      "type": "string",
      "description": "string"
    },
    "disabled": {
      "type": "boolean",
      "description": "string"
    },
    "table": {
      "type": "string",
      "description": "string"
    },
    "query": {
      "type": "array",
      "description": "Mutli-line SQL query as an array, each line in query is a new array element"
    }
  }
}
